# Convert character vector to a factor and display levels:
char_vector <- c("male", "female", "male", "female")
factor_vector <- factor(char_vector)
print(factor_vector)
print(levels(factor_vector))
# output 
[1] male   female male   female
Levels

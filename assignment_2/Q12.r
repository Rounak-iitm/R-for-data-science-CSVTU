# 12. Use a fourfold repetition of the second row of the matrix formed in (11.)

array_12 <- array(rep(result_11[2, ], each = 4), dim = c(2, 2, 2, 3))
print(array_12)

# Output: A 2 x 2 x 2 x 3 array
